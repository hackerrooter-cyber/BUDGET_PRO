// Pont sécurisé entre le contexte de rendu et Electron.
// Aucun API Node n'est exposé pour conserver l'isolement du contexte.
window.addEventListener('DOMContentLoaded', () => {
  // Future hooks si nécessaires.
});
